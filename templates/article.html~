{% extends "base.html" %}
{% block title %}{{article.title}}{% endblock %}
{% load url from future %}
{% block content %}
<script>
function my_callback(data){
    var like=document.getElementById("likesTag");
    like.innerHTML=data.likes_num;
    //alert(data.message);
}


</script> 
<h1>{{article.title}} </h1>
<p> {{article.body|safe}} </p>
<p id="likesTag">{{article.likes}}</p> 
<p>poeple liked this article</p>

<input type="button" onclick="Dajaxice.article.likeJax(my_callback, {'data1': {{article.id}} } )" value="LIKE!!">

<hr size =10px >
{% for commnet in commnets%}
	
	<p> writer : {{ commnet.writer }} </p>
	<p> text : {{ commnet.body}} </p>
	<p> date : {{ commnet.date }} </p>
	<hr width = 40%>
{% endfor %}

<form method="POST">{% csrf_token %}
    <div id="cform">
            Name: {{ comment_form.writer }}
            <p>{{ comment_form.body }}</p>
    </div>
    <div id="submit"><input type="submit" value="Submit"></div>
</form>
{% endblock %}

